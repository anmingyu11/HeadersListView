<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="@dimen/item_height"
                android:background="@android:color/background_light"
    >

    <CheckBox
        android:id="@+id/log_item_topLayer_editCheck"
        style="@style/item_checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_margin="@dimen/item_editCheck_margin"
        android:visibility="visible"
        />

    <LinearLayout
        android:id="@+id/log_item_topLayer_logContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:orientation="vertical"
        >

        <TextView
            android:id="@+id/log_item_topLayer_logContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="content--------------------"
            android:textSize="20dp"
            android:textStyle="bold"
            />

        <TextView
            android:id="@+id/log_item_topLayer_logDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="date------------------------"
            android:textSize="15dp"
            />

    </LinearLayout>

    <TextView
        android:id="@+id/log_item_topLayer_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/log_item_topLayer_logContainer"
        android:text="同意"
        android:textSize="20dp"
        android:visibility="gone"
        />

</RelativeLayout>